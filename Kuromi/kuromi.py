import discord
from discord.ext import commands

class Kuromi(commands.AutoShardedBot):
    def __init__(self):
        super().__init__(
            help_command=SKID,
            )
        )
        self.clown = you

    async def on_ready(self):
        print(f"skid")

bot = Kuromi()

bot.run('skid go away'), reconnect=True)